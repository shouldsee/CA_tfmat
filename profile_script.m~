profile off
profile on

sys0.critname='batch1';
sys0.crit=@(odr,d) odr>0.4 && min(d)<0.7; ;

lst0=1:2^18;
idx=randperm(numel(lst0));
mlst0=reshape(lst0(idx),[2^8 2^10]);
for bchi=1:2^10
fprintf('batch %d of %d\n',batch,bmax)    
lst=mlst0(bi);
end
derrida_general
profile viewer
profsave
